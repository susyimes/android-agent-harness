# Provenance and privacy inventory

Inventory date: 2026-07-16 (Asia/Shanghai)

## Reference scope

`D:\mirror-android` was treated as read-only architectural evidence at revision `59e09bc1d0752aba7a06aef6be5b3cd107d90dfd`. Its working tree already contained unrelated modifications in `AgentToolOrchestrator.kt` and its test. Only the orchestrator declaration shape was queried; no changed implementation was extracted.

The following reference files or declaration sets were inspected:

- `docs/PROJECT_ARCHITECTURE.md`
- root `build.gradle`, `settings.gradle`, `gradle.properties`, and `gradle/wrapper/gradle-wrapper.properties`
- `app/build.gradle` (build structure only; no properties file was opened)
- `app/src/main/java/com/mirror/android/agent/AgentChatClient.kt` (contract/model declarations and provider boundary)
- `AgentRuntimeContext.kt`, `ChatModels.kt`, and `MirrorChatRepository.kt` declarations
- `orchestrator/AgentRunModels.kt` and `AgentMirrorContextProvider.kt`
- `context/AgentContextModels.kt` declarations
- `tool/AgentToolModels.kt`, `AgentToolRegistry.kt`, and `AgentToolOrchestrator.kt` declarations
- `house/AgentHouseModels.kt`, `AgentHarnessRunner.kt`, and `AgentHarnessRunnerTest.kt`

No top-level license covering the reference application code was found during the inventory. Accordingly, no implementation text, package name, resource, data layout, asset, or product configuration was copied. The target contracts and sample were authored afresh from the task's required seams and the high-level boundary map above.

## Excluded material

The following were neither opened nor copied:

- `secrets.properties`, `local.properties`, `.env`, tokens, credentials, keystores, signing material, or account data
- app-private files, chat/session data, memory, todos, usage statistics, sensor/location data, debug traces, caches, or user content
- product API endpoints, provider defaults, model identifiers, headers, allowlists, or deployment configuration
- icons, images, audio, fonts, layouts, branding, prompts, personas, skills, native libraries, model files, or other product-only assets
- generated build directories, IDE metadata, local Maven contents, third-party source trees, and Git history contents

## Target inventory

| Category | M0 contents | Privacy/provenance result |
| --- | --- | --- |
| Core source | New generic interfaces, models, in-memory store, registry, and deterministic runner | No reference namespace or implementation copied |
| Android sample | Programmatic UI, public placeholder text, scripted local provider, uppercase tool | No permissions, network, files, database, telemetry, branding, or binary assets |
| Configuration | Public Gradle plugin/SDK versions and JVM settings | No endpoint, credential field, signing config, or local machine path |
| Test data | Fixed ids, timestamps, and strings such as `android` | Synthetic only; no user or device data |
| Documentation | Compatibility names and the reference revision for auditability | Contains reference names only as provenance facts, never as runtime configuration |
| License | Apache-2.0 for this repository | Explicit open-source grant |
| Gradle wrapper | Standard files generated by Gradle 8.11.1 | Third-party build bootstrap only; see `THIRD_PARTY_NOTICES.md` |

## Automated guard

The repository-native `auditProvenance` task fails when it finds:

- common secret/config/signing filenames anywhere outside ignored build/cache directories;
- embedded credential-like assignments or reference product namespaces in source/build files; or
- non-XML binary resources in the Android sample.

This guard complements the manual inventory; it is not a general-purpose secret scanner. Publication should still review `git diff --cached` and the complete tracked-file list.

