plugins {
    id 'org.jetbrains.kotlin.jvm'
}

java {
    sourceCompatibility JavaVersion.VERSION_17
    targetCompatibility JavaVersion.VERSION_17
}

tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).configureEach {
    kotlinOptions {
        jvmTarget = '17'
    }
}

dependencies {
    testImplementation "dev.androidagent.harness:agent-sdk:${rootProject.version}"
    testImplementation "dev.androidagent.harness:agent-approval:${rootProject.version}"
    testImplementation "dev.androidagent.harness:context-engine:${rootProject.version}"
    testImplementation "dev.androidagent.harness:agent-state:${rootProject.version}"
    testImplementation "dev.androidagent.harness:agent-scheduling:${rootProject.version}"
    testImplementation "dev.androidagent.harness:agent-feedback:${rootProject.version}"
    testImplementation "dev.androidagent.harness:provider-openai:${rootProject.version}"
    testImplementation "dev.androidagent.harness:device-loop:${rootProject.version}"
    testImplementation "dev.androidagent.harness:harness-eval:${rootProject.version}"
    testImplementation 'junit:junit:4.13.2'
}

tasks.withType(Test).configureEach {
    useJUnit()
}

tasks.named('compileTestKotlin') {
    dependsOn rootProject.tasks.named('publishJvmSdk')
}
